import Vue from 'vue'
import Vuex from 'vuex'

import UserStore from './models/user'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    UserStore
  }
})
